<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
    </style>

    <!-- NoActionbar application theme -->
    <style name="NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- AlertDialog theme -->
    <style name="AlertDialogTheme">
        <item name="android:background">@color/customPrimary</item>
        <item name="android:textColor">@color/grey_shade</item>
    </style>

    <!-- Settings fragment theme -->
    <style name="SettingsFragmentTheme">
        <item name="android:background">@color/customPrimary</item>
    </style>

    <style name="AlertDialog_Dark" parent="Theme.AppCompat.Dialog.Alert"><!--BOTTONI-->
        <item name="android:backgroundTint" tools:targetApi="lollipop">
           @color/customPrimary
        </item>
    </style>

    <style name="PopupMenu" parent="@android:style/Widget.PopupMenu">
        <item name="android:popupBackground">@color/customPrimary</item>
        <item name="android:textColor">@color/grey_shade</item>
    </style>

</resources>
